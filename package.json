{
  "name": "vt-legend",
  "version": "1.0.0",
  "description": "Create legends for vector tile styles",
  "scripts": {
    "test": "vitest --coverage --run --environment happy-dom",
    "start": "parcel src/index.html",
    "build": "parcel build --public-url . src/index.html",
    "lint": "npx eslint src/**/*.js",
    "lint:fix": "npx eslint --fix src/**/*.js"
  },
  "license": "",
  "dependencies": {
    "html-to-image": "^1.9.0",
    "maplibre-gl": "^2.1.9"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "c8": "^7.11.3",
    "eslint": "^8.15.0",
    "happy-dom": "^5.3.2",
    "parcel": "^2.6.0",
    "process": "^0.11.10",
    "vitest": "^0.16.0"
  }
}
